name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    name: Build & Test
    permissions:
      contents: read
      security-events: write
    uses: ./.github/workflows/reusable-build.yml
