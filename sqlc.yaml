version: "2"
sql:
  - engine: postgresql
    queries: sql/queries/
    schema: migrations/
    gen:
      go:
        package: db
        out: internal/db/sqlc
        emit_json_tags: true
        emit_pointers_for_null_types: true
        emit_prepared_queries: false
        sql_package: pgx/v5
